import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { router } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';
import { Card, PrimaryButton } from '@/components/ui'; import { JOBS } from '@/config/balance'; import { useGameStore } from '@/store/gameStore'; import { colors, spacing } from '@/theme';
export default function Earn() { const work = useGameStore(s => s.work); return <ScrollView style={s.root} contentContainerStyle={s.content}><Text onPress={() => router.back()} style={s.back}>‹ Back</Text><Text style={s.title}>Earn Money</Text><Text style={s.subtitle}>Build your first €1,000 through real work.</Text>{JOBS.map(job => <Card key={job.id} style={s.job}><View style={s.row}><Ionicons name={job.icon} size={25} color={colors.gold} /><View style={s.info}><Text style={s.name}>{job.name}</Text><Text style={s.meta}>{job.durationDays} day · €{job.minReward}–€{job.maxReward}</Text></View></View><PrimaryButton title="ACCEPT JOB" onPress={() => { work(job.id); router.back(); }} /></Card>)}</ScrollView> }
const s=StyleSheet.create({root:{flex:1,backgroundColor:colors.bg},content:{padding:spacing.md,paddingTop:55,gap:12},back:{color:colors.gold,fontWeight:'800',fontSize:17},title:{color:colors.text,fontSize:34,fontWeight:'900',marginTop:10},subtitle:{color:colors.muted,marginBottom:10},job:{gap:16},row:{flexDirection:'row',alignItems:'center'},info:{marginLeft:14},name:{color:colors.text,fontSize:17,fontWeight:'800'},meta:{color:colors.muted,marginTop:4}});
